# API Стены и Раздела "О себе" Профиля для iOS приложения

Данный документ описывает API для работы со стеной пользователя и отображения раздела "О себе" в профиле.

## Базовый URL

```
https://k-connect.ru
```

---

## 1. Что такое "Стена" и чем она отличается от постов

### 1.1. Стена (Wall)

**Стена** — это специальная лента записей на профиле пользователя, куда другие пользователи могут оставлять публичные сообщения.

**Характеристики стены:**
- Посты на стене имеют тип `type='stena'`
- У поста на стене есть `recipient_id` — ID владельца стены
- Посты на стене создаются другими пользователями (не владельцем профиля)
- Посты на стене видны всем, кто имеет доступ к профилю
- Владелец стены может удалять записи со своей стены

### 1.2. Обычные посты (Posts)

**Обычные посты** — это записи, которые пользователь создает сам на своем профиле.

**Характеристики обычных постов:**
- Посты имеют тип `type='post'` или `type=null`
- Посты не имеют `recipient_id` (или `recipient_id=null`)
- Посты создаются владельцем профиля
- Посты отображаются в разделе "Посты" профиля
- Посты на стене (`type='stena'`) исключаются из обычной ленты постов

### 1.3. Различия

| Характеристика | Стена | Обычные посты |
|----------------|-------|---------------|
| **Автор** | Другие пользователи | Владелец профиля |
| **Тип поста** | `type='stena'` | `type='post'` или `null` |
| **Получатель** | `recipient_id` = ID владельца стены | `recipient_id` = `null` |
| **Раздел** | Вкладка "Стена" | Вкладка "Посты" |
| **Кто может создавать** | Любой авторизованный пользователь | Только владелец профиля |
| **Ограничения** | 1 запись в час на стене одного пользователя | 5 постов в час, 1 пост в 20 секунд |

---

## 2. Кто может оставлять записи на стене

### 2.1. Права доступа

**Могут оставлять записи на стене:**
- ✅ Любой авторизованный пользователь (кроме владельца профиля)
- ✅ Пользователи, которые подписаны на владельца стены
- ✅ Пользователи, на которых подписан владелец стены
- ✅ Взаимные подписчики (друзья)

**Не могут оставлять записи:**
- ❌ Владелец профиля (не может писать на свою стену)
- ❌ Неавторизованные пользователи
- ❌ Пользователи, которые заблокированы владельцем стены

### 2.2. Ограничения

**Rate Limiting для записей на стене:**
- **1 запись в час** на стене одного пользователя
- Если пользователь уже оставил запись на стене пользователя, следующую запись можно оставить только через 1 час

**Пример ошибки:**
```json
{
  "success": false,
  "error": "Вы уже оставили запись на стене этого пользователя. Следующую запись можно оставить через 45 мин. 30 сек.",
  "rate_limit": {
    "reset": 1234567890,
    "remaining": 0
  }
}
```

### 2.3. Почему CreatePost видно для всех

**CreatePost компонент отображается для всех пользователей** на вкладке "Стена" по следующим причинам:

1. **Удобство использования:** Любой пользователь может быстро оставить запись на стене
2. **Единый интерфейс:** Один компонент используется для создания обычных постов и записей на стене
3. **Условная логика:** При создании поста на стене передаются параметры:
   - `postType='stena'` — указывает, что это запись на стену
   - `recipientId={user.id}` — ID владельца стены

**Важно:** Владелец профиля не может писать на свою стену — это проверяется на бэкенде, и запрос будет отклонен.

---

## 3. API для работы со стеной

### 3.1. Получение записей стены

#### 3.1.1. Эндпоинт

```
GET /api/profile/<user_identifier>/wall
```

#### 3.1.2. Заголовки запроса

```
Authorization: Bearer <JWT_TOKEN>
User-Agent: KConnect-iOS/1.0 (iPhone; iOS 26.2; Scale/3.0)
X-Mobile-Client: true
```

#### 3.1.3. Параметры запроса

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `page` | Integer | Нет | Номер страницы (по умолчанию: 1) |
| `per_page` | Integer | Нет | Количество постов на странице (по умолчанию: 10, максимум: 50) |

#### 3.1.4. Успешный ответ (200 OK)

```json
{
  "posts": [
    {
      "id": 12345,
      "content": "Привет! Как дела?",
      "type": "stena",
      "recipient_id": 1234567890123456,
      "timestamp": "2024-01-15T10:30:00Z",
      "image": null,
      "video": null,
      "views_count": 10,
      "likes_count": 5,
      "comments_count": 2,
      "reposts_count": 0,
      "user": {
        "id": 9876543210987654,
        "name": "Имя Автора",
        "username": "author_username",
        "photo": "photo.jpg",
        "avatar_url": "https://s3.k-connect.ru/static/uploads/avatar/9876543210987654/photo.jpg",
        "verification": {
          "status": 1,
          "date": "2024-01-01"
        },
        "achievement": {
          "bage": "Gold",
          "image_path": "https://s3.k-connect.ru/static/images/bages/gold.png"
        }
      },
      "reactions_summary": {
        "👍": 3,
        "❤️": 2
      },
      "user_reaction": "👍"
    }
  ],
  "page": 1,
  "per_page": 10,
  "total": 25,
  "has_next": true,
  "has_prev": false,
  "pages": 3
}
```

#### 3.1.5. Ошибки

**403 Forbidden - Приватный профиль:**
```json
{
  "error": "This profile is private. Only mutual friends can view posts.",
  "is_private": true
}
```

**404 Not Found - Пользователь не найден:**
```json
{
  "error": "User not found"
}
```

### 3.2. Создание записи на стене

#### 3.2.1. Эндпоинт

```
POST /api/posts
```

#### 3.2.2. Заголовки запроса

```
Authorization: Bearer <JWT_TOKEN>
Content-Type: multipart/form-data
User-Agent: KConnect-iOS/1.0 (iPhone; iOS 26.2; Scale/3.0)
X-Mobile-Client: true
```

#### 3.2.3. Параметры запроса (FormData)

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `content` | String | Нет | Текст записи |
| `type` | String | Да | Должно быть `'stena'` |
| `recipient_id` | Integer | Да | ID владельца стены |
| `image` | File | Нет | Изображение (PNG, JPG, JPEG, GIF) |
| `video` | File | Нет | Видео (MP4, MOV, AVI) |

#### 3.2.4. Успешный ответ (200 OK)

```json
{
  "success": true,
  "post": {
    "id": 12345,
    "content": "Привет! Как дела?",
    "type": "stena",
    "recipient_id": 1234567890123456,
    "timestamp": "2024-01-15T10:30:00Z",
    "user": {
      "id": 9876543210987654,
      "name": "Имя Автора",
      "username": "author_username"
    }
  }
}
```

#### 3.2.5. Ошибки

**400 Bad Request - Неверные параметры:**
```json
{
  "success": false,
  "error": "Необходимо указать recipient_id для записи на стену"
}
```

**429 Too Many Requests - Превышен лимит:**
```json
{
  "success": false,
  "error": "Вы уже оставили запись на стене этого пользователя. Следующую запись можно оставить через 45 мин. 30 сек.",
  "rate_limit": {
    "reset": 1234567890,
    "remaining": 0
  }
}
```

**403 Forbidden - Нельзя писать на свою стену:**
```json
{
  "success": false,
  "error": "Вы не можете оставить запись на своей стене"
}
```

### 3.3. Удаление записи со стены

**Права на удаление:**
- ✅ Автор записи может удалить свою запись
- ✅ Владелец стены может удалить любую запись со своей стены

**Эндпоинт:**
```
POST /api/posts/<post_id>/delete
```

**Успешный ответ:**
```json
{
  "success": true,
  "message": "Пост успешно удален"
}
```

---

## 4. Раздел "О себе" (About)

### 4.1. Получение данных профиля

#### 4.1.1. Эндпоинт

```
GET /api/profile/<user_identifier>
```

#### 4.1.2. Заголовки запроса

```
Authorization: Bearer <JWT_TOKEN>
User-Agent: KConnect-iOS/1.0 (iPhone; iOS 26.2; Scale/3.0)
X-Mobile-Client: true
```

#### 4.1.3. Успешный ответ (200 OK)

```json
{
  "user": {
    "id": 1234567890123456,
    "name": "Имя Пользователя",
    "username": "username",
    "about": "О себе: люблю музыку, программирование и путешествия",
    "location": "Москва, Россия",
    "website": "https://example.com",
    "birthday": "1990-05-15",
    "registration_date": "2020-01-15T10:30:00Z",
    "interests": "Музыка, Программирование, Путешествия",
    "avatar_url": "https://s3.k-connect.ru/static/uploads/avatar/1234567890123456/photo.jpg",
    "banner_url": "https://s3.k-connect.ru/static/uploads/banner/1234567890123456/banner.jpg",
    "profile_background_url": "https://s3.k-connect.ru/static/uploads/prof_back/1234567890123456/background.jpg",
    "followers_count": 1234,
    "following_count": 567,
    "friends_count": 89,
    "posts_count": 456,
    "photos_count": 234,
    "total_likes": 5678,
    "verification_status": 1,
    "verification": {
      "status": 1,
      "date": "2024-01-01"
    },
    "achievement": {
      "bage": "Gold",
      "image_path": "https://s3.k-connect.ru/static/images/bages/gold.png",
      "upgrade": "2",
      "color_upgrade": "#FFD700"
    },
    "subscription": {
      "type": "ultimate",
      "subscription_date": "2024-01-01T00:00:00Z",
      "expires_at": "2024-12-31T23:59:59Z",
      "total_duration_months": 12,
      "active": true
    },
    "purchased_usernames": [
      {
        "id": 1,
        "username": "cool_username",
        "price_paid": 1000,
        "purchase_date": "2024-01-10T00:00:00Z",
        "is_active": false
      }
    ],
    "is_private": false,
    "account_type": "user"
  },
  "socials": [
    {
      "name": "Instagram",
      "link": "https://instagram.com/username",
      "title": "Instagram",
      "url": "https://instagram.com/username",
      "color": "#E4405F",
      "icon": null
    },
    {
      "name": "VK",
      "link": "https://vk.com/username",
      "title": "VKontakte",
      "url": "https://vk.com/username",
      "color": "#0077FF",
      "icon": null
    }
  ],
  "connect_info": [
    {
      "connection_date": "2024-01-01T00:00:00Z",
      "days": 365,
      "is_mutual": true,
      "status": "connected",
      "type": "friend",
      "username": "friend_username"
    }
  ],
  "is_following": false,
  "is_friend": true,
  "notifications_enabled": false
}
```

### 4.2. Получение статистики профиля

#### 4.2.1. Эндпоинт

```
GET /api/profile/<user_id>/stats
```

#### 4.2.2. Успешный ответ (200 OK)

```json
{
  "avg_likes_per_post": 12.5,
  "days_active": 365,
  "posts_count": 456,
  "total_likes": 5678
}
```

### 4.3. Структура данных раздела "О себе"

#### 4.3.1. Основная информация

| Поле | Тип | Описание | Пример |
|------|-----|----------|--------|
| `about` | String \| null | Текст "О себе" | "О себе: люблю музыку..." |
| `location` | String \| null | Местоположение | "Москва, Россия" |
| `website` | String \| null | Веб-сайт | "https://example.com" |
| `birthday` | String \| null | Дата рождения (ISO 8601) | "1990-05-15" |
| `registration_date` | String \| null | Дата регистрации (ISO 8601) | "2020-01-15T10:30:00Z" |
| `interests` | String \| null | Интересы | "Музыка, Программирование" |

#### 4.3.2. Социальные сети

```json
{
  "socials": [
    {
      "name": "Instagram",
      "link": "https://instagram.com/username",
      "title": "Instagram",
      "url": "https://instagram.com/username",
      "color": "#E4405F",
      "icon": null
    }
  ]
}
```

**Поддерживаемые социальные сети:**
- Instagram
- Facebook
- Twitter/X
- VK (VKontakte)
- YouTube
- Telegram
- Element
- Другие (с кастомными иконками)

#### 4.3.3. Купленные юзернеймы

```json
{
  "purchased_usernames": [
    {
      "id": 1,
      "username": "cool_username",
      "price_paid": 1000,
      "purchase_date": "2024-01-10T00:00:00Z",
      "is_active": false
    }
  ]
}
```

#### 4.3.4. Информация о коннекте (Connect Info)

```json
{
  "connect_info": [
    {
      "connection_date": "2024-01-01T00:00:00Z",
      "days": 365,
      "is_mutual": true,
      "status": "connected",
      "type": "friend",
      "username": "friend_username"
    }
  ]
}
```

**Поля Connect Info:**
- `connection_date` — дата установления связи
- `days` — количество дней с момента связи
- `is_mutual` — взаимная ли связь
- `status` — статус связи
- `type` — тип связи (friend, follow, etc.)
- `username` — username связанного пользователя

#### 4.3.5. Статистика

```json
{
  "avg_likes_per_post": 12.5,
  "days_active": 365,
  "posts_count": 456,
  "total_likes": 5678
}
```

**Поля статистики:**
- `avg_likes_per_post` — среднее количество лайков на пост
- `days_active` — количество дней активности
- `posts_count` — общее количество постов
- `total_likes` — общее количество лайков

### 4.4. Отображение раздела "О себе"

#### 4.4.1. Структура компонента

Раздел "О себе" должен отображать следующие элементы:

1. **Информация о коннекте** (если есть)
   - Формат: `@{username} • {days} дней • @{friend_username}`
   - Ссылки на профили пользователей

2. **Текст "О себе"** (`about`)
   - Отображается с цветом статуса пользователя (если есть)
   - Поддерживает Markdown форматирование

3. **Местоположение** (`location`)
   - Иконка: LocationOnIcon
   - Отображается как текст

4. **Веб-сайт** (`website`)
   - Иконка: LinkIcon
   - Отображается как ссылка (без протокола)

5. **Дата рождения** (`birthday`)
   - Иконка: CakeIcon
   - Форматируется как дата

6. **Дата регистрации** (`registration_date`)
   - Иконка: TodayIcon
   - Форматируется как "15 января 2020"

7. **Купленные юзернеймы** (`purchased_usernames`)
   - Иконка: AlternateEmailIcon
   - Отображаются как чипсы (chips)
   - Активный юзернейм выделяется

8. **Социальные сети** (`socials`)
   - Отображаются как иконки с ссылками
   - Поддерживаются кастомные иконки

9. **Статистика** (опционально, закомментировано)
   - Среднее количество лайков на пост
   - Дни активности
   - Количество постов
   - Общее количество лайков

#### 4.4.2. Проверка доступа

**Приватный профиль:**
- Если профиль приватный (`is_private: true`) и пользователь не является владельцем или другом, показывается сообщение:
  ```
  Приватный профиль
  Подпишитесь друг на друга для просмотра информации
  ```

**Доступ к информации:**
- Владелец профиля — полный доступ
- Друзья (взаимные подписчики) — полный доступ
- Остальные пользователи — ограниченный доступ (если профиль не приватный)

---

## 5. Примеры использования

### 5.1. Получение записей стены

**Запрос:**
```
GET /api/profile/username/wall?page=1&per_page=10
Authorization: Bearer <JWT_TOKEN>
```

**Ответ:**
```json
{
  "posts": [
    {
      "id": 12345,
      "content": "Привет! Как дела?",
      "type": "stena",
      "recipient_id": 1234567890123456,
      "timestamp": "2024-01-15T10:30:00Z",
      "user": {
        "id": 9876543210987654,
        "name": "Имя Автора",
        "username": "author_username"
      }
    }
  ],
  "page": 1,
  "per_page": 10,
  "total": 25,
  "has_next": true,
  "has_prev": false,
  "pages": 3
}
```

### 5.2. Создание записи на стене

**Запрос:**
```
POST /api/posts
Content-Type: multipart/form-data
Authorization: Bearer <JWT_TOKEN>

FormData:
  content: "Привет! Как дела?"
  type: "stena"
  recipient_id: 1234567890123456
```

**Ответ:**
```json
{
  "success": true,
  "post": {
    "id": 12345,
    "content": "Привет! Как дела?",
    "type": "stena",
    "recipient_id": 1234567890123456,
    "timestamp": "2024-01-15T10:30:00Z"
  }
}
```

### 5.3. Получение данных профиля (раздел "О себе")

**Запрос:**
```
GET /api/profile/username
Authorization: Bearer <JWT_TOKEN>
```

**Ответ:**
```json
{
  "user": {
    "id": 1234567890123456,
    "name": "Имя Пользователя",
    "username": "username",
    "about": "О себе: люблю музыку, программирование и путешествия",
    "location": "Москва, Россия",
    "website": "https://example.com",
    "birthday": "1990-05-15",
    "registration_date": "2020-01-15T10:30:00Z",
    "interests": "Музыка, Программирование, Путешествия",
    "purchased_usernames": [
      {
        "id": 1,
        "username": "cool_username",
        "price_paid": 1000,
        "purchase_date": "2024-01-10T00:00:00Z",
        "is_active": false
      }
    ]
  },
  "socials": [
    {
      "name": "Instagram",
      "link": "https://instagram.com/username",
      "title": "Instagram",
      "url": "https://instagram.com/username",
      "color": "#E4405F"
    }
  ],
  "connect_info": [
    {
      "connection_date": "2024-01-01T00:00:00Z",
      "days": 365,
      "is_mutual": true,
      "status": "connected",
      "type": "friend",
      "username": "friend_username"
    }
  ]
}
```

### 5.4. Получение статистики

**Запрос:**
```
GET /api/profile/1234567890123456/stats
Authorization: Bearer <JWT_TOKEN>
```

**Ответ:**
```json
{
  "avg_likes_per_post": 12.5,
  "days_active": 365,
  "posts_count": 456,
  "total_likes": 5678
}
```

---

## 6. Важные замечания

1. **Разделение постов и стены:**
   - Посты на стене (`type='stena'`) исключаются из обычной ленты постов
   - Обычные посты (`type='post'` или `null`) исключаются из стены

2. **Rate Limiting:**
   - Записи на стене: 1 запись в час на стене одного пользователя
   - Обычные посты: 5 постов в час, 1 пост в 20 секунд

3. **Права доступа:**
   - Владелец профиля не может писать на свою стену
   - Владелец стены может удалять любые записи со своей стены
   - Автор записи может удалить свою запись

4. **Приватность:**
   - Приватные профили ограничивают доступ к стене и разделу "О себе"
   - Только взаимные подписчики (друзья) имеют полный доступ

5. **CreatePost компонент:**
   - Отображается для всех на вкладке "Стена"
   - При создании поста на стене передаются `postType='stena'` и `recipientId`
   - Владелец профиля не может создать пост на своей стене (проверка на бэкенде)

---

## 7. Чек-лист реализации

### Стена:
- [ ] Реализовано получение записей стены
- [ ] Реализовано создание записи на стене
- [ ] Реализовано удаление записи со стены
- [ ] Реализована обработка rate limiting (1 запись в час)
- [ ] Реализована проверка прав доступа
- [ ] Реализована пагинация записей стены
- [ ] Реализована обработка ошибок (403, 404, 429)

### Раздел "О себе":
- [ ] Реализовано получение данных профиля
- [ ] Реализовано отображение информации о коннекте
- [ ] Реализовано отображение текста "О себе"
- [ ] Реализовано отображение местоположения
- [ ] Реализовано отображение веб-сайта
- [ ] Реализовано отображение даты рождения
- [ ] Реализовано отображение даты регистрации
- [ ] Реализовано отображение купленных юзернеймов
- [ ] Реализовано отображение социальных сетей
- [ ] Реализовано получение и отображение статистики
- [ ] Реализована проверка доступа для приватных профилей

---

## Поддержка

При возникновении проблем:
1. Проверьте валидность JWT токена
2. Проверьте права доступа к профилю (приватность)
3. Проверьте rate limiting для записей на стене
4. Проверьте наличие обязательных параметров (`type='stena'`, `recipient_id`)
5. Проверьте логи сервера для детальной диагностики

